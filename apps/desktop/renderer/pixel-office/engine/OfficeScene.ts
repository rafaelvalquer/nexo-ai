import { Container,Graphics,Sprite,Text,Texture } from "pixi.js";import type { AgentVisualEvent,OfficeStationId } from "@nexo/shared";import { AgentSprite } from "../agent/AgentSprite";import { AgentMovement } from "../agent/AgentMovement";import { AgentActionQueue,type AgentAction } from "../agent/AgentActionQueue";import { Grid } from "../navigation/Grid";import { findPath } from "../navigation/AStar";import { smoothPath } from "../navigation/PathSmoothing";import { OFFICE_STATIONS } from "../data/office-layout";import type { OctopusAnimation } from "../data/sprite-manifest";
export class OfficeScene extends Container{readonly agent:AgentSprite;private grid=new Grid();private movement=new AgentMovement();private queue=new AgentActionQueue();private current?:AgentAction;private currentSince=0;private station:OfficeStationId="central-desk";private statusLabel=new Text({text:"Disponível",style:{fontFamily:"monospace",fontSize:22,fill:"#ffffff",stroke:{color:"#121827",width:5}}});constructor(map:Texture,sheet:Texture,onAgent:()=>void,onStation:(station:OfficeStationId)=>void){super();this.sortableChildren=true;const background=new Sprite(map);background.zIndex=0;this.addChild(background);for(const station of Object.values(OFFICE_STATIONS)){const hit=new Graphics().circle(0,0,34).fill({color:0x7c5cff,alpha:.001});hit.position.set(station.x,station.y);hit.eventMode="static";hit.cursor="pointer";hit.on("pointertap",()=>onStation(station.id));hit.zIndex=station.y-1;this.addChild(hit);}this.agent=new AgentSprite(sheet);this.agent.position.set(OFFICE_STATIONS[this.station].x,OFFICE_STATIONS[this.station].y);this.agent.zIndex=this.agent.y;this.agent.on("pointertap",onAgent);this.addChild(this.agent);this.statusLabel.anchor.set(.5,1);this.statusLabel.position.set(this.agent.x,this.agent.y-92);this.statusLabel.zIndex=2000;this.addChild(this.statusLabel);}consume(event:AgentVisualEvent,reducedMotion=false){if(["run.completed","run.failed","run.cancelled"].includes(event.type))this.queue.clear();this.queue.enqueue(event,this.station);if(!this.current)this.startNext(reducedMotion);}update(delta:number,reducedMotion=false){if(!this.current){this.agent.update(delta);return;}if(this.current.kind==="move"){if(reducedMotion){const target=OFFICE_STATIONS[this.current.stationId];this.agent.position.set(target.x,target.y);this.finishAction(reducedMotion);}else{const result=this.movement.update(this.agent.position,delta);this.agent.play((`walk${result.direction[0].toUpperCase()}${result.direction.slice(1)}`) as OctopusAnimation);if(result.arrived)this.finishAction(reducedMotion);}}else if(Date.now()-this.currentSince>=this.current.minimumMs&&this.queue.size()>0)this.finishAction(reducedMotion);this.agent.zIndex=this.agent.y;this.statusLabel.position.set(this.agent.x,this.agent.y-92);this.agent.update(delta);}private startNext(reducedMotion:boolean){this.current=this.queue.next();if(!this.current){this.agent.play("idle");return;}this.currentSince=Date.now();this.statusLabel.text=this.current.label;if(this.current.kind==="move"){const target=OFFICE_STATIONS[this.current.stationId];const raw=findPath(this.grid,this.grid.toGrid(this.agent.x,this.agent.y),this.grid.toGrid(target.x,target.y));if(!raw.length){this.statusLabel.text="Não foi possível alcançar a estação";this.current=undefined;this.agent.play("error");return;}const path=smoothPath(raw).map(point=>this.grid.toWorld(point));path.push({x:target.x,y:target.y});this.movement.setPath(path);if(reducedMotion){this.agent.position.set(target.x,target.y);this.finishAction(true);}}else this.agent.play(this.current.state==="walking"?"idle":this.current.state as OctopusAnimation);}private finishAction(reducedMotion:boolean){if(this.current)this.station=this.current.stationId;this.current=undefined;this.startNext(reducedMotion);}getDebug(){return{station:this.station,x:Math.round(this.agent.x),y:Math.round(this.agent.y),pathNodes:this.movement.path.length,queue:this.queue.size()};}}

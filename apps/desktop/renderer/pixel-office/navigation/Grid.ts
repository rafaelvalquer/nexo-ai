import layout from "../data/navigation-grid.json";
export type GridPoint={x:number;y:number};export class Grid{readonly cellSize=layout.cellSize;readonly width=layout.width;readonly height=layout.height;private blocked=new Set(layout.blocked.map(([x,y])=>`${x}:${y}`));isWalkable(point:GridPoint){return point.x>=0&&point.y>=0&&point.x<this.width&&point.y<this.height&&!this.blocked.has(`${point.x}:${point.y}`);}neighbors(point:GridPoint){return[{x:point.x+1,y:point.y},{x:point.x-1,y:point.y},{x:point.x,y:point.y+1},{x:point.x,y:point.y-1}].filter(candidate=>this.isWalkable(candidate));}toGrid(x:number,y:number){return{x:Math.max(0,Math.min(this.width-1,Math.round(x/this.cellSize))),y:Math.max(0,Math.min(this.height-1,Math.round(y/this.cellSize)))}}toWorld(point:GridPoint){return{x:point.x*this.cellSize,y:point.y*this.cellSize}}toggle(point:GridPoint){const key=`${point.x}:${point.y}`;this.blocked.has(key)?this.blocked.delete(key):this.blocked.add(key);}blockedCells(){return[...this.blocked].map(key=>{const[x,y]=key.split(":").map(Number);return{x,y};});}}

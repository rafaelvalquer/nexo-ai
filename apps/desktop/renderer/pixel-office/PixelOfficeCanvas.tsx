import { useCallback,useEffect,useRef,useState } from "react";import type { OfficeStationId } from "@nexo/shared";import { OfficeEngine } from "./engine/OfficeEngine";import { useOfficeStore } from "./state/office-store";import { useOfficePerformance } from "./hooks/useOfficePerformance";
export function PixelOfficeCanvas({onAgent,onStation}:{onAgent:()=>void;onStation:(station:OfficeStationId)=>void}){const host=useRef<HTMLDivElement>(null),engine=useRef<OfficeEngine|undefined>(undefined),event=useOfficeStore(state=>state.event),zoom=useOfficeStore(state=>state.zoom),reduced=useOfficeStore(state=>state.reducedMotion),developer=useOfficeStore(state=>state.developerMode);const[error,setError]=useState<string>(),[debug,setDebug]=useState<Record<string,unknown>>({});useEffect(()=>{const instance=new OfficeEngine();engine.current=instance;let active=true;void instance.mount(host.current!,onAgent,onStation).catch(reason=>active&&setError(reason instanceof Error?reason.message:String(reason)));return()=>{active=false;instance.destroy();engine.current=undefined;};},[onAgent,onStation]);useEffect(()=>{if(event)engine.current?.consume(event);},[event?.eventId]);useEffect(()=>engine.current?.setReducedMotion(reduced),[reduced]);useEffect(()=>{if(host.current)engine.current?.setZoom(host.current,zoom);},[zoom]);const visibility=useCallback((visible:boolean)=>engine.current?.setVisible(visible),[]);useOfficePerformance(visibility);useEffect(()=>{if(!developer)return;const timer=window.setInterval(()=>setDebug(engine.current?.debug()??{}),500);return()=>window.clearInterval(timer);},[developer]);if(error)return <div className="officeFallback" role="alert"><h2>Pixel Office não pôde ser carregado</h2><p>{error}</p><button onClick={()=>location.reload()}>Recarregar</button></div>;return <div className="officeCanvas" ref={host} role="img" aria-label={`Escritório virtual do Nexo. Estado atual: ${event?.label??"Disponível"}`}>{developer&&<pre className="developerOverlay">{Object.entries(debug).map(([key,value])=>`${key}: ${value}`).join("\n")}</pre>}</div>;}

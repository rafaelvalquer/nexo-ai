import { useMemo } from "react";import { useOfficeStore } from "../state/office-store";export function AgentStatus(){const state=useOfficeStore();const elapsed=useMemo(()=>state.event?Math.max(0,Math.floor((Date.now()-Date.parse(state.event.timestamp))/1000)):0,[state.event]);return <footer className={`officeStatus ${state.severity}`} aria-live="polite"><div className="statusAvatar">🐙</div><div><b>Nexo <span>● {state.state==="idle"?"Disponível":state.state}</span></b><p>{state.label}</p></div><div className="statusMeta"><span>{state.stationId}</span><span>{elapsed}s · {state.eventCount} eventos</span>{state.backgroundTasks>0&&<span>{state.backgroundTasks} tarefa(s) em segundo plano</span>}</div></footer>;}

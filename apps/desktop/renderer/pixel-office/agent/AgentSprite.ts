import { Container,Rectangle,Sprite,Texture } from "pixi.js";import { OCTOPUS_SPRITE,type OctopusAnimation } from "../data/sprite-manifest";
export class AgentSprite extends Container{private frames=new Map<OctopusAnimation,Texture[]>();private sprite=new Sprite();private animation:OctopusAnimation="idle";private frame=0;private elapsed=0;constructor(sheet:Texture){super();for(const[name,indexes]of Object.entries(OCTOPUS_SPRITE.animations) as [OctopusAnimation,number[]][]){this.frames.set(name,indexes.map(index=>{const col=index%4,row=Math.floor(index/4),x=OCTOPUS_SPRITE.frameColumns[col],y=OCTOPUS_SPRITE.frameRows[row],w=OCTOPUS_SPRITE.frameColumns[col+1]-x,h=OCTOPUS_SPRITE.frameRows[row+1]-y;return new Texture({source:sheet.source,frame:new Rectangle(x,y,w,h)});}));}this.sprite.anchor.set(.5,.82);this.sprite.scale.set(.34);this.addChild(this.sprite);this.play("idle");this.eventMode="static";this.cursor="pointer";}play(animation:OctopusAnimation){if(this.animation===animation)return;this.animation=animation;this.frame=0;this.elapsed=0;this.apply();}update(deltaSeconds:number){const fps=this.animation.startsWith("walk")?9:["success","responding","working"].includes(this.animation)?7:3;this.elapsed+=deltaSeconds;if(this.elapsed>=1/fps){this.elapsed=0;const frames=this.frames.get(this.animation)!;this.frame=(this.frame+1)%frames.length;this.apply();}}private apply(){this.sprite.texture=this.frames.get(this.animation)?.[this.frame]??Texture.EMPTY;}}

import type { AgentVisualEvent,OfficeStationId } from "@nexo/shared";import { stateForEvent,type AgentAnimationState } from "./AgentStateMachine";
export type AgentAction={kind:"move"|"animate";stationId:OfficeStationId;state:AgentAnimationState;label:string;minimumMs:number;event:AgentVisualEvent};
export class AgentActionQueue{private actions:AgentAction[]=[];enqueue(event:AgentVisualEvent,current:OfficeStationId){const station=event.stationId??"central-desk",mapped=stateForEvent(event);if(station!==current)this.actions.push({kind:"move",stationId:station,state:"walking",label:`Indo para ${event.label}`,minimumMs:0,event});const state:AgentAnimationState=event.type==="tool.started"?"working":event.type==="approval.requested"?"approval":mapped;const minimumMs=state==="success"?700:state==="error"?1000:state==="working"?350:0;this.actions.push({kind:"animate",stationId:station,state,label:event.label,minimumMs,event});}next(){return this.actions.shift();}clear(){this.actions=[];}size(){return this.actions.length;}}

import type { AgentVisualEvent,OfficeStationId } from "@nexo/shared";import { stateForEvent,type AgentAnimationState } from "./AgentStateMachine";
export type AgentAction={kind:"move"|"animate";stationId:OfficeStationId;state:AgentAnimationState;label:string;minimumMs:number;event:AgentVisualEvent};
export const STATE_DURATION={success:700,error:1400,cancelled:1000} as const;
export class AgentActionQueue{private actions:AgentAction[]=[];private activeMoveTarget?:OfficeStationId;enqueue(event:AgentVisualEvent,current:OfficeStationId){const station=event.stationId??"central-desk",mapped=stateForEvent(event);if(station!==current&&station!==this.activeMoveTarget&&!this.hasMoveTo(station))this.actions.push({kind:"move",stationId:station,state:"walking",label:`Indo para ${event.label}`,minimumMs:0,event});const state:AgentAnimationState=event.type==="tool.started"?"working":event.type==="approval.requested"?"approval":mapped;const minimumMs=state in STATE_DURATION?STATE_DURATION[state as keyof typeof STATE_DURATION]:state==="working"?350:0;this.actions.push({kind:"animate",stationId:station,state,label:event.label,minimumMs,event});}next(){const action=this.actions.shift();if(action?.kind==="move")this.activeMoveTarget=action.stationId;return action;}completeMove(station:OfficeStationId){if(this.activeMoveTarget===station)this.activeMoveTarget=undefined;}hasMoveTo(station:OfficeStationId){return this.actions.some(action=>action.kind==="move"&&action.stationId===station);}clear(){this.actions=[];this.activeMoveTarget=undefined;}size(){return this.actions.length;}debug(){return{activeMoveTarget:this.activeMoveTarget,queuedMoveTargets:this.actions.filter(action=>action.kind==="move").map(action=>action.stationId)};}}

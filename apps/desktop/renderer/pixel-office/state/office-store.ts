import { create } from "zustand";import type { AgentVisualEvent,AgentVisualSnapshot } from "@nexo/shared";import { initialOfficeState,visualEventReducer,type OfficeVisualState } from "../events/event-reducer";import { VisualRunCoordinator } from "../events/VisualRunCoordinator";
const coordinator=new VisualRunCoordinator();type OfficeStore=OfficeVisualState&{recent:AgentVisualEvent[];backgroundTasks:number;developerMode:boolean;reducedMotion:boolean;zoom:"fit"|1|1.25|1.5;apply:(event:AgentVisualEvent)=>void;hydrate:(snapshot:AgentVisualSnapshot)=>void;setDeveloperMode:(value:boolean)=>void;setReducedMotion:(value:boolean)=>void;setZoom:(value:OfficeStore["zoom"])=>void};
const prefersReducedMotion=typeof matchMedia==="function"&&matchMedia("(prefers-reduced-motion: reduce)").matches;
export const useOfficeStore=create<OfficeStore>(set=>({...initialOfficeState,recent:[],backgroundTasks:0,developerMode:false,reducedMotion:prefersReducedMotion,zoom:"fit",apply:event=>set(state=>{const active=coordinator.push(event);return active?{...visualEventReducer(state,active),recent:[...state.recent,event].slice(-20),backgroundTasks:coordinator.backgroundCount()}:{recent:state.recent,backgroundTasks:0};}),hydrate:snapshot=>set(state=>{const active=coordinator.restore(snapshot.recent);return active?{...visualEventReducer(state,active),recent:snapshot.recent,backgroundTasks:coordinator.backgroundCount()}:{recent:snapshot.recent,backgroundTasks:0};}),setDeveloperMode:developerMode=>set({developerMode}),setReducedMotion:reducedMotion=>set({reducedMotion}),setZoom:zoom=>set({zoom})}));

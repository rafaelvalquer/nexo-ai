import type { ChatMessage } from "@nexo/shared";import { motion } from "motion/react";import { Sparkles } from "lucide-react";import { MessageContent } from "./MessageContent";
export function AssistantMessage({message,grouped=false}:{message:ChatMessage;grouped?:boolean}){return <motion.article initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} transition={{duration:.18}} className={`chatMessage ${message.role}${grouped?" grouped":""}`}><div className="messageIdentity"><Sparkles size={15}/>{message.role==="system"?"Sistema":"Nexo"}</div><div className="messageContent"><MessageContent message={message}/></div></motion.article>;}

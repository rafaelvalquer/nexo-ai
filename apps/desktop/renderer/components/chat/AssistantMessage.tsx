import type { ChatMessage } from "@nexo/shared";import { motion } from "motion/react";import { Sparkles } from "lucide-react";import { MarkdownContent } from "./MarkdownContent";import { ToolResultRenderer } from "./ToolResultRenderer";
export function AssistantMessage({message}:{message:ChatMessage}){return <motion.article initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} transition={{duration:.18}} className={`chatMessage ${message.role}`}><div className="messageIdentity"><Sparkles size={15}/>{message.role==="system"?"Sistema":"Nexo"}</div><div className="messageContent"><ToolResultRenderer content={message.content}/><MarkdownContent content={message.content}/></div></motion.article>;}

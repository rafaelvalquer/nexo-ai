import type { BackgroundTask } from "@nexo/shared";import { Sparkles } from "lucide-react";import { MarkdownContent } from "./MarkdownContent";import { ExecutionSummary } from "./ExecutionSummary";
export function StreamingMessage({task,elapsed}:{task:BackgroundTask;elapsed:number}){const history=task.statusHistory?.length?task.statusHistory:[task.statusMessage??"Preparando resposta…"];return <article className="chatMessage assistant streaming"><div className="messageIdentity"><Sparkles size={15}/>Nexo</div><ExecutionSummary history={history} elapsed={elapsed}/>{task.progressText?<div className="messageContent"><MarkdownContent content={task.progressText}/><i className="streamCursor"/></div>:<div className="typing"><span/><span/><span/><em>Nexo está preparando a resposta…</em></div>}</article>;}

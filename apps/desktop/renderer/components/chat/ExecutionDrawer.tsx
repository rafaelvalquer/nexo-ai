import { lazy, Suspense } from "react";import type { BackgroundTask } from "@nexo/shared";import { X } from "lucide-react";import { ExecutionSummary } from "./ExecutionSummary";
const AgentFlowView=lazy(()=>import("../ai/flow/AgentFlowView").then(module=>({default:module.AgentFlowView})));
export function ExecutionDrawer({task,elapsed,open,onClose}:{task?:BackgroundTask;elapsed:number;open:boolean;onClose:()=>void}){if(!open)return null;const history=task?.statusHistory?.length?task.statusHistory:[task?.statusMessage??"Nenhuma execução ativa."];return <div className="drawerBackdrop" onMouseDown={onClose}><aside className="executionDrawer" onMouseDown={event=>event.stopPropagation()} aria-label="Detalhes da execução"><header><div><small>AGENTE LOCAL</small><h2>Execução</h2></div><button onClick={onClose} aria-label="Fechar"><X size={18}/></button></header><Suspense fallback={<div className="agentFlowLoading">Preparando fluxo de execução…</div>}><AgentFlowView task={task}/></Suspense><ExecutionSummary history={history} elapsed={elapsed}/></aside></div>;}

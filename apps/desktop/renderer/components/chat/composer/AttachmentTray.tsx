import { AnimatePresence,motion } from "motion/react";import { FileText,LoaderCircle,X } from "lucide-react";import type { AssistantAttachment } from "../../../stores/assistant";
export function AttachmentTray({items,onRemove}:{items:AssistantAttachment[];onRemove:(id:string)=>void}){return <div className="attachmentTray"><AnimatePresence>{items.map(item=><motion.div className={`attachmentItem ${item.status}`} key={item.taskId} initial={{opacity:0,scale:.95}} animate={{opacity:1,scale:1}} exit={{opacity:0,scale:.95}}><FileText size={15}/><span>{item.name}<small>{item.status==="indexing"?<><LoaderCircle size={11}/>Indexando…</>:item.status==="ready"?"✓ Pronto":"Falha"}</small></span><button onClick={()=>onRemove(item.taskId)} aria-label={`Remover ${item.name}`}><X size={13}/></button></motion.div>)}</AnimatePresence></div>;}

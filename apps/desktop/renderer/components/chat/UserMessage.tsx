import type { ChatMessage } from "@nexo/shared";import { motion } from "motion/react";import { MarkdownContent } from "./MarkdownContent";
export function UserMessage({message,grouped=false}:{message:ChatMessage;grouped?:boolean}){return <motion.article initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} transition={{duration:.18}} className={`chatMessage user${grouped?" grouped":""}`}><div className="messageIdentity">Você</div><div className="messageContent"><MarkdownContent content={message.content}/></div></motion.article>;}

import { useEffect, useMemo, useState } from "react";
import { Search } from "lucide-react";
import { useAppStore } from "../../stores/app";
const commands = ["Hoje","Assistente","Documentos","Conexões","Aprovações","Automações","Atividade","Memória","Configurações"];
export function CommandPalette() { const [open,setOpen]=useState(false),[query,setQuery]=useState(""); const setPage=useAppStore(s=>s.setPage); useEffect(()=>{const key=(event:KeyboardEvent)=>{if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==="k"){event.preventDefault();setOpen(value=>!value);}if(event.key==="Escape")setOpen(false);};window.addEventListener("keydown",key);return()=>window.removeEventListener("keydown",key);},[]); const visible=useMemo(()=>commands.filter(command=>command.toLowerCase().includes(query.toLowerCase())),[query]); if(!open)return null; return <div className="paletteOverlay" onMouseDown={()=>setOpen(false)}><div className="palette" role="dialog" aria-modal="true" aria-label="Paleta de comandos" onMouseDown={event=>event.stopPropagation()}><div className="paletteSearch"><Search size={16}/><input autoFocus value={query} onChange={event=>setQuery(event.target.value)} placeholder="Navegar ou procurar comando…"/></div><div className="paletteList">{visible.map(command=><button key={command} className="ghost" onClick={()=>{setPage(command);setOpen(false);setQuery("");}}><span>{command}</span><kbd>Ir</kbd></button>)}</div><small>Ctrl+K para abrir · Esc para fechar</small></div></div>; }

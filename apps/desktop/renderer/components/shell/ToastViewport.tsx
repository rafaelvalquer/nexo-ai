import { AnimatePresence,motion } from "motion/react";
import { AlertCircle,Check,Info,X } from "lucide-react";
import { useToastStore } from "../../stores/toast";
export function ToastViewport(){const items=useToastStore(state=>state.items),dismiss=useToastStore(state=>state.dismiss);return <div className="toastViewport" aria-live="polite" aria-relevant="additions"><AnimatePresence initial={false}>{items.map(item=>{const Icon=item.tone==="success"?Check:item.tone==="error"?AlertCircle:Info;return <motion.article key={item.id} className={`nexoToast ${item.tone}`} role={item.tone==="error"?"alert":"status"} initial={{opacity:0,x:18,y:4}} animate={{opacity:1,x:0,y:0}} exit={{opacity:0,x:12}} transition={{duration:.18}}><span className="toastIcon"><Icon size={16}/></span><span className="toastCopy"><b>{item.title}</b>{item.description&&<small>{item.description}</small>}</span><button onClick={()=>dismiss(item.id)} aria-label="Dispensar notificação"><X size={14}/></button></motion.article>;})}</AnimatePresence></div>;}

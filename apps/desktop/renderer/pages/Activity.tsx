import { useEffect, useState } from "react";
export function Activity(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{window.nexo.listAudit().then(setRows)},[]);return <div><header><div><h1>Atividade</h1><p>Todas as ações do Nexo ficam auditáveis.</p></div></header><section className="panel list">{rows.length===0?<div className="empty">Ainda não há atividade.</div>:rows.map(r=><div className="row" key={r.id}><div><b>{r.action}</b><span>{new Date(r.createdAt).toLocaleString()} · {r.risk}</span></div><span className={`tag ${r.status}`}>{r.status}</span></div>)}</section></div>}

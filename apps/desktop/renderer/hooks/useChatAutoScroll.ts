import { useCallback,useEffect,useState,type RefObject } from "react";
export function useChatAutoScroll(ref:RefObject<HTMLDivElement>,dependency:unknown,streaming:boolean){const[nearBottom,setNearBottom]=useState(true),[unread,setUnread]=useState(false);const scrollToBottom=useCallback((behavior:ScrollBehavior="smooth")=>{const node=ref.current;if(node)node.scrollTo({top:node.scrollHeight,behavior});setNearBottom(true);setUnread(false);},[ref]);const onScroll=useCallback(()=>{const node=ref.current;if(!node)return;const near=node.scrollHeight-node.scrollTop-node.clientHeight<=120;setNearBottom(near);if(near)setUnread(false);},[ref]);useEffect(()=>{if(nearBottom)scrollToBottom(streaming?"auto":"smooth");else if(streaming)setUnread(true);},[dependency,nearBottom,scrollToBottom,streaming]);return{isNearBottom:nearBottom,hasUnreadBelow:unread,onScroll,scrollToBottom};}

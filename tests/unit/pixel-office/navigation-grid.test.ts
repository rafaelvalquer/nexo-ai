import { describe,expect,it } from "vitest";import { Grid } from "../../../apps/desktop/renderer/pixel-office/navigation/Grid";import { OFFICE_STATIONS } from "../../../apps/desktop/renderer/pixel-office/data/office-layout";
describe("grade de navegação",()=>{it("mantém todas as estações alcançáveis",()=>{const grid=new Grid();for(const station of Object.values(OFFICE_STATIONS))expect(grid.isWalkable(grid.toGrid(station.x,station.y)),station.id).toBe(true);});it("converte mundo e grade de forma estável",()=>{const grid=new Grid();expect(grid.toGrid(760,650)).toEqual({x:12,y:10});expect(grid.toWorld({x:12,y:10})).toEqual({x:768,y:640});});});

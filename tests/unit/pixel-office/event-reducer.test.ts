import { describe,expect,it } from "vitest";import { initialOfficeState,visualEventReducer } from "../../../apps/desktop/renderer/pixel-office/events/event-reducer";import { event } from "./fixtures";
describe("reducer visual",()=>{it("atualiza tarefa, estação, rótulo e severidade atomicamente",()=>{const next=visualEventReducer(initialOfficeState,event("tool.progress",{stationId:"document-station",label:"Lendo contrato",severity:"warning"}));expect(next).toMatchObject({runId:"run-1",stationId:"document-station",label:"Lendo contrato",severity:"warning",state:"working",eventCount:1});});});

import {describe,expect,it} from "vitest";import {evalLoop} from "./eval-harness.js";describe("agent eval failures",()=>{it("stops after consecutive failures",async()=>{const call={toolCalls:[{id:"1",name:"tool_a",arguments:{}}]};const h=evalLoop([call,call,call],{failPreflight:true});const state=await h.loop.run("fail");expect(state.status).toBe("FAILED");expect(state.consecutiveFailures).toBe(3);});});

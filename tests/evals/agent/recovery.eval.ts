import {describe,expect,it} from "vitest";import {evalLoop} from "./eval-harness.js";describe("agent eval recovery",()=>{it("persists a resumable approval state",async()=>{const saved:any[]=[];const h=evalLoop([{toolCalls:[{id:"1",name:"tool_a",arguments:{}}]}],{mutation:true});const original=(h.loop as any).deps;(h.loop as any).deps={...original,persistence:{save:async(state:any)=>saved.push(structuredClone(state))}};await h.loop.run("recover");expect(saved.some(state=>state.status==="WAITING_APPROVAL")).toBe(true);});});

import {describe,expect,it} from "vitest";import {evalLoop} from "./eval-harness.js";describe("agent eval unavailable tool",()=>{it("fails closed when preflight denies the call",async()=>{const h=evalLoop(Array.from({length:3},(_,i)=>({toolCalls:[{id:String(i),name:"tool_a",arguments:{}}]})),{failPreflight:true});expect((await h.loop.run("denied")).status).toBe("FAILED");expect(h.calls).toHaveLength(0);});});

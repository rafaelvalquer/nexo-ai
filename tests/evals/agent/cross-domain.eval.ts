import {describe,expect,it} from "vitest";import {evalLoop} from "./eval-harness.js";describe("agent eval cross domain",()=>{it("changes tool domain across turns",async()=>{const h=evalLoop([{toolCalls:[{id:"1",name:"tool_a",arguments:{}}]},{toolCalls:[{id:"2",name:"tool_b",arguments:{}}]},{content:"combined",toolCalls:[]}]);expect((await h.loop.run("cross")).finalResponse).toBe("combined");expect(h.calls).toEqual(["tool_a","tool_b"]);});});

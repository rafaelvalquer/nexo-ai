import {describe,expect,it} from "vitest";import {evalLoop} from "./eval-harness.js";describe("agent eval multi step",()=>{it("chains three tools without an intent plan",async()=>{const h=evalLoop([{toolCalls:[{id:"1",name:"tool_a",arguments:{step:1}}]},{toolCalls:[{id:"2",name:"tool_b",arguments:{step:2}}]},{toolCalls:[{id:"3",name:"tool_a",arguments:{step:3}}]},{content:"done",toolCalls:[]}]);const state=await h.loop.run("three");expect(state.status).toBe("COMPLETED");expect(h.calls).toHaveLength(3);});});

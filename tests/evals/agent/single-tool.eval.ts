import {describe,expect,it} from "vitest";import {evalLoop} from "./eval-harness.js";describe("agent eval single tool",()=>{it("selects and executes one tool",async()=>{const h=evalLoop([{toolCalls:[{id:"1",name:"tool_a",arguments:{}}]},{content:"done",toolCalls:[]}]);const state=await h.loop.run("one");expect(state.status).toBe("COMPLETED");expect(h.calls).toEqual(["tool_a"]);});});

import {describe,expect,it} from "vitest";import {evalLoop} from "./eval-harness.js";describe("agent eval approvals",()=>{it("interrupts before mutation",async()=>{const h=evalLoop([{toolCalls:[{id:"1",name:"tool_a",arguments:{}}]}],{mutation:true});const state=await h.loop.run("mutate");expect(state.status).toBe("WAITING_APPROVAL");expect(h.calls).toHaveLength(0);expect(state.pendingAction?.executionId).toBeTruthy();});});

import {describe,expect,it,vi} from "vitest";import {OllamaProvider} from "../../../packages/core/src/llm/ollama.js";describe("agent eval structured fallback",()=>{it("uses structured planning when native tools are rejected",async()=>{const provider=new OllamaProvider("http://local","model");const plan=vi.spyOn(provider,"planStructured").mockResolvedValue({type:"final",content:"fallback"} as never);const fetchMock=vi.spyOn(globalThis,"fetch").mockResolvedValue(new Response("",{status:422}));try{const result=await provider.agentTurn({messages:[{role:"user",content:"hello"}],tools:[]});expect(plan).toHaveBeenCalledOnce();expect(result).toEqual({content:"fallback",toolCalls:[]});}finally{fetchMock.mockRestore();plan.mockRestore();}});});

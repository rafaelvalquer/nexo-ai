import {describe,expect,it} from "vitest";import {evalLoop} from "./eval-harness.js";describe("agent eval loop guard",()=>{it("detects repeated actions",async()=>{const repeated=Array.from({length:4},(_,i)=>({toolCalls:[{id:String(i),name:"tool_a",arguments:{same:true}}]}));const h=evalLoop(repeated);const state=await h.loop.run("loop");expect(state.status).toBe("FAILED");expect(state.finalResponse).toMatch(/repetição|estagnação|stagnation/i);});});

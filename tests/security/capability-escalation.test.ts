import {describe,expect,it} from "vitest";import {z} from "zod";import {ToolRegistry} from "../../packages/core/src/tools/registry.js";import {PermissionEngine} from "../../packages/core/src/permissions/policy.js";import {ActionExecutor} from "../../packages/core/src/agent/execution/action-executor.js";const audit:any={record(){}};
describe("capability escalation",()=>{it("rejects connection capabilities when no resolver is configured",async()=>{const registry=new ToolRegistry().register({name:"email_secret",description:"private",risk:"READ",permissions:["email.read"],inputSchema:z.object({}),execute:async()=>({ok:true,summary:"bad"})});const executor=new ActionExecutor(registry,new PermissionEngine(()=>({allowedRoots:[],autonomy:"balanced"} as any)),audit);expect(await executor.preflight("email_secret",{})).toMatchObject({ok:false,code:"CAPABILITY_DENIED"});});});

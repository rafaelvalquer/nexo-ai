import {describe,expect,it} from "vitest";import {z} from "zod";import {ToolRegistry} from "../../packages/core/src/tools/registry.js";import {CapabilityAwareToolCatalog} from "../../packages/core/src/agent/orchestrator/tool-catalog.js";import {ShadowAgent} from "../../packages/core/src/agent/loop/shadow-agent.js";
describe("shadow mutation",()=>{it("decides but has no execution path",async()=>{let calls=0;const registry=new ToolRegistry().register({name:"danger",description:"danger",risk:"CRITICAL",permissions:[],mutatesState:true,inputSchema:z.object({}),execute:async()=>{calls++;return{ok:true,summary:"bad"};}});const llm:any={agentTurn:async()=>({toolCalls:[{id:"c",name:"danger",arguments:{}}]})};const decision=await new ShadowAgent(llm,new CapabilityAwareToolCatalog(registry)).decide("do it");expect(decision).toMatchObject({kind:"tool",toolName:"danger",mutatesState:true});expect(calls).toBe(0);});});

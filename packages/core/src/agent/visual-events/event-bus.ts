import { randomUUID } from "node:crypto";import type { AgentVisualEvent,AgentVisualSnapshot } from "@nexo/shared";
type Draft=Omit<AgentVisualEvent,"eventId"|"timestamp"|"agentId">;
export class VisualEventBus{private listeners=new Set<(event:AgentVisualEvent)=>void>();private recent:AgentVisualEvent[]=[];emit(draft:Draft){const event:AgentVisualEvent={...draft,eventId:randomUUID(),agentId:"nexo-main",timestamp:new Date().toISOString()};this.recent=[...this.recent,event].slice(-50);for(const listener of this.listeners)listener(event);return event;}subscribe(listener:(event:AgentVisualEvent)=>void){this.listeners.add(listener);return()=>this.listeners.delete(listener);}snapshot():AgentVisualSnapshot{return{current:this.recent.at(-1),recent:[...this.recent]};}}

import type { ToolResult } from "@nexo/shared";
import type { AgentObservation, AgentReference } from "./types.js";
const refKeys: Record<string, AgentReference["kind"]> = { id: "id", path: "path", url: "url", messageId: "message_id", eventId: "event_id", threadId: "thread_id", fileId: "file_id", processId: "process_id" };
export function encodeObservation(toolCallId: string, toolName: string, result: ToolResult, trust: AgentObservation["trust"] = "UNTRUSTED_CONTENT", maxBytes = 64_000): AgentObservation { const references = collectReferences(result.data); const serialized = JSON.stringify(result.data); const originalBytes = Buffer.byteLength(serialized); const truncated = originalBytes > maxBytes; const data = truncated ? { references, notice: "Conteúdo truncado; use referências preservadas." } : result.data; return { toolCallId, toolName, ok: result.ok, summary: result.summary, ...(data === undefined ? {} : { data }), ...(references.length ? { references } : {}), trust, truncated, originalBytes, encodedBytes: Buffer.byteLength(JSON.stringify(data)) }; }
function collectReferences(value: unknown, found: AgentReference[] = []): AgentReference[] { if (!value || typeof value !== "object" || found.length >= 100) return found; if (Array.isArray(value)) { value.forEach(item => collectReferences(item, found)); return found; } for (const [key, item] of Object.entries(value as Record<string, unknown>)) { const kind = refKeys[key]; if (kind && (typeof item === "string" || typeof item === "number")) found.push({ kind, value: String(item), label: key }); else collectReferences(item, found); } return found; }

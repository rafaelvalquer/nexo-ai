import type { ConnectionService } from "../connections/service.js";
import type { EmailDraft, EmailDraftInput, EmailMessage, EmailSearchQuery, EmailSearchResult, EmailModifyAction, EmailAttachment } from "./types.js";
import { randomUUID } from "node:crypto";
import fs from "node:fs/promises";

export class EmailService {
  constructor(private connections: ConnectionService) {}
  async search(input: EmailSearchQuery): Promise<EmailSearchResult> { const account = this.connections.get(input.connectionId); if (!account) throw new Error("Conexão não encontrada."); const token = await this.connections.accessToken(account.id, "email.read"); return account.provider === "google" ? searchGoogle(token, input) : searchMicrosoft(token, input); }
  async getMessage(connectionId:string, messageId:string): Promise<EmailMessage> { const account=this.connections.get(connectionId); if(!account) throw new Error("Conexão não encontrada."); const token=await this.connections.accessToken(connectionId,"email.read"); const response=account.provider === "google" ? await fetch(`https://gmail.googleapis.com/gmail/v1/users/me/messages/${encodeURIComponent(messageId)}?format=full`,{headers:{Authorization:`Bearer ${token}`}}) : await fetch(`https://graph.microsoft.com/v1.0/me/messages/${encodeURIComponent(messageId)}?$select=id,subject,from,toRecipients,ccRecipients,receivedDateTime,body,bodyPreview,isRead,hasAttachments,conversationId`,{headers:{Authorization:`Bearer ${token}`}}); if(!response.ok) throw new Error("Não foi possível obter o e-mail."); const item=await response.json() as any; return account.provider === "google" ? normalizeGoogleMessage(item) : normalizeMicrosoftMessage(item); }
  async getThread(connectionId:string, threadId:string): Promise<EmailMessage[]> { const account=this.connections.get(connectionId); if(!account) throw new Error("Conexão não encontrada."); const token=await this.connections.accessToken(connectionId,"email.read"); const url=account.provider==="google" ? `https://gmail.googleapis.com/gmail/v1/users/me/threads/${encodeURIComponent(threadId)}?format=full` : `https://graph.microsoft.com/v1.0/me/messages?$filter=conversationId%20eq%20'${encodeURIComponent(threadId)}'&$orderby=receivedDateTime%20asc`; const response=await fetch(url,{headers:{Authorization:`Bearer ${token}`}}); if(!response.ok) throw new Error("Não foi possível obter a conversa."); const data=await response.json() as any; const messages=account.provider==="google" ? data.messages??[] : data.value??[]; return messages.map((item:any)=>account.provider==="google"?normalizeGoogleMessage(item):normalizeMicrosoftMessage(item)); }
  async listAttachments(connectionId:string,messageId:string): Promise<EmailAttachment[]> { const account=this.connections.get(connectionId); if(!account) throw new Error("Conexão não encontrada."); const token=await this.connections.accessToken(connectionId,"email.read"); const url=account.provider==="google" ? `https://gmail.googleapis.com/gmail/v1/users/me/messages/${encodeURIComponent(messageId)}?format=full` : `https://graph.microsoft.com/v1.0/me/messages/${encodeURIComponent(messageId)}/attachments`; const response=await fetch(url,{headers:{Authorization:`Bearer ${token}`}}); if(!response.ok) throw new Error("Não foi possível listar anexos."); const data=await response.json() as any; return account.provider==="google" ? gmailAttachments(data.payload) : (data.value??[]).map((item:any)=>({id:item.id,name:item.name??"anexo",contentType:item.contentType,size:item.size})); }
  async downloadAttachment(connectionId:string,messageId:string,attachmentId:string,destination:string) { const account=this.connections.get(connectionId); if(!account) throw new Error("Conexão não encontrada."); const token=await this.connections.accessToken(connectionId,"email.read"); const response=account.provider==="google" ? await fetch(`https://gmail.googleapis.com/gmail/v1/users/me/messages/${encodeURIComponent(messageId)}/attachments/${encodeURIComponent(attachmentId)}`,{headers:{Authorization:`Bearer ${token}`}}) : await fetch(`https://graph.microsoft.com/v1.0/me/messages/${encodeURIComponent(messageId)}/attachments/${encodeURIComponent(attachmentId)}/$value`,{headers:{Authorization:`Bearer ${token}`}}); if(!response.ok) throw new Error("Não foi possível baixar o anexo."); const bytes=account.provider==="google" ? Buffer.from((await response.json() as any).data??"","base64url") : Buffer.from(await response.arrayBuffer()); if(!bytes.length) throw new Error("O anexo não possui conteúdo para salvar."); await fs.writeFile(destination,bytes,{flag:"wx"}); return {destination,size:bytes.length}; }
  async createDraft(input: EmailDraftInput): Promise<EmailDraft> { const account = this.connections.get(input.connectionId); if (!account) throw new Error("Conexão não encontrada."); await this.connections.accessToken(account.id, "email.send"); return { id: randomUUID(), provider: account.provider, message: input }; }
  async modify(connectionId:string, messageId:string, action:EmailModifyAction, value?:string) { const account=this.connections.get(connectionId); if(!account) throw new Error("Conexão não encontrada."); const token=await this.connections.accessToken(connectionId,"email.modify"); const response=account.provider === "google" ? await modifyGoogle(token,messageId,action,value) : await modifyMicrosoft(token,messageId,action,value); if(!response.ok) throw new Error("O provedor recusou a alteração do e-mail."); return {ok:true,provider:account.provider,action}; }
  async sendDraft(draft: EmailDraft) { const token = await this.connections.accessToken(draft.message.connectionId, "email.send"); if (draft.provider === "google") { const payload = [`To: ${draft.message.to.map(x=>x.email).join(", ")}`, `Subject: ${draft.message.subject}`, "", draft.message.bodyText].join("\r\n"); const raw = Buffer.from(payload).toString("base64url"); const response = await fetch("https://gmail.googleapis.com/gmail/v1/users/me/messages/send", { method:"POST", headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"}, body:JSON.stringify({raw}) }); if (!response.ok) throw new Error("O Gmail recusou o envio."); return { ok:true, provider:"google" }; }
    const response = await fetch("https://graph.microsoft.com/v1.0/me/sendMail", { method:"POST", headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"}, body:JSON.stringify({message:{subject:draft.message.subject,body:{contentType:"Text",content:draft.message.bodyText},toRecipients:draft.message.to.map(x=>({emailAddress:{address:x.email,name:x.name}})),ccRecipients:draft.message.cc?.map(x=>({emailAddress:{address:x.email,name:x.name}}))},saveToSentItems:true}) }); if (!response.ok) throw new Error("O Microsoft Graph recusou o envio."); return { ok:true, provider:"microsoft" };
  }
}
async function modifyGoogle(token:string,id:string,action:EmailModifyAction,value?:string) { const headers={Authorization:`Bearer ${token}`,"Content-Type":"application/json"}; if(action==="trash") return fetch(`https://gmail.googleapis.com/gmail/v1/users/me/messages/${encodeURIComponent(id)}/trash`,{method:"POST",headers}); if((action==="move"||action==="add_label"||action==="remove_label")&&!value) throw new Error("Informe o marcador ou destino do e-mail."); const body=action==="mark_read"?{removeLabelIds:["UNREAD"]}:action==="mark_unread"?{addLabelIds:["UNREAD"]}:action==="archive"?{removeLabelIds:["INBOX"]}:action==="flag"?{addLabelIds:["STARRED"]}:action==="move"?{removeLabelIds:["INBOX"],addLabelIds:[value!]}:action==="add_label"?{addLabelIds:[value!]}:{removeLabelIds:[value!]}; return fetch(`https://gmail.googleapis.com/gmail/v1/users/me/messages/${encodeURIComponent(id)}/modify`,{method:"POST",headers,body:JSON.stringify(body)}); }
async function modifyMicrosoft(token:string,id:string,action:EmailModifyAction,value?:string) { const headers={Authorization:`Bearer ${token}`,"Content-Type":"application/json"}; const base=`https://graph.microsoft.com/v1.0/me/messages/${encodeURIComponent(id)}`; if(action==="mark_read"||action==="mark_unread") return fetch(base,{method:"PATCH",headers,body:JSON.stringify({isRead:action==="mark_read"})}); if(action==="flag") return fetch(base,{method:"PATCH",headers,body:JSON.stringify({flag:{flagStatus:"flagged"}})}); if(action==="add_label"||action==="remove_label") { if(!value) throw new Error("Informe a categoria do e-mail."); const current=await fetch(`${base}?$select=categories`,{headers}); if(!current.ok) return current; const categories=((await current.json() as any).categories??[]) as string[]; const next=action==="add_label"?[...new Set([...categories,value])]:categories.filter(category=>category!==value); return fetch(base,{method:"PATCH",headers,body:JSON.stringify({categories:next})}); } const destinationId=action==="archive"?"archive":action==="move"?value:"deleteditems"; if(!destinationId) throw new Error("Informe a pasta de destino do e-mail."); return fetch(`${base}/move`,{method:"POST",headers,body:JSON.stringify({destinationId})}); }
async function searchGoogle(token:string, input:EmailSearchQuery): Promise<EmailSearchResult> { const query = new URLSearchParams({ maxResults:String(Math.min(input.maxResults ?? 20, 50)), q:[input.unread ? "is:unread" : "", input.query ?? ""].filter(Boolean).join(" ") }); if (input.pageToken) query.set("pageToken", input.pageToken); const listed = await fetch(`https://gmail.googleapis.com/gmail/v1/users/me/messages?${query}`, {headers:{Authorization:`Bearer ${token}`}}); if (!listed.ok) throw new Error("Não foi possível consultar o Gmail."); const listedData = await listed.json() as any; const messages = listedData.messages ?? []; const hydrated = await Promise.all(messages.slice(0,20).map(async ({id}:any) => { const response = await fetch(`https://gmail.googleapis.com/gmail/v1/users/me/messages/${id}?format=metadata&metadataHeaders=From&metadataHeaders=To&metadataHeaders=Subject`, {headers:{Authorization:`Bearer ${token}`}}); const item = await response.json() as any; const headers = Object.fromEntries((item.payload?.headers ?? []).map((h:any)=>[h.name.toLowerCase(),h.value])); return {id:item.id,provider:"google",threadId:item.threadId,from:{email:headers.from ?? ""},to:[{email:headers.to ?? ""}],subject:headers.subject ?? "(sem assunto)",receivedAt:new Date(Number(item.internalDate)).toISOString(),snippet:item.snippet,isUnread:(item.labelIds ?? []).includes("UNREAD"),hasAttachments:false} satisfies EmailMessage; })); return { messages: hydrated, nextPageToken: listedData.nextPageToken, total: listedData.resultSizeEstimate }; }
async function searchMicrosoft(token:string, input:EmailSearchQuery): Promise<EmailSearchResult> { const params = new URLSearchParams({ "$top":String(Math.min(input.maxResults ?? 20,50)), "$select":"id,subject,from,toRecipients,receivedDateTime,bodyPreview,isRead,hasAttachments", "$orderby":"receivedDateTime desc" }); if (input.unread) params.set("$filter", "isRead eq false"); if (input.query?.trim()) params.set("$search", `\"${input.query.trim().replace(/\"/g, "")}\"`); const url = input.pageToken ? trustedGraphNextLink(input.pageToken) : `https://graph.microsoft.com/v1.0/me/messages?${params}`; const response = await fetch(url, {headers:{Authorization:`Bearer ${token}`, ...(input.query?.trim() ? {ConsistencyLevel:"eventual"} : {})}}); if (!response.ok) throw new Error("Não foi possível consultar o Microsoft Graph."); const data = await response.json() as any; return { messages:(data.value ?? []).map((item:any) => ({id:item.id,provider:"microsoft",from:{email:item.from?.emailAddress?.address ?? "",name:item.from?.emailAddress?.name},to:(item.toRecipients ?? []).map((x:any)=>({email:x.emailAddress.address,name:x.emailAddress.name})),subject:item.subject ?? "(sem assunto)",receivedAt:item.receivedDateTime,snippet:item.bodyPreview,isUnread:!item.isRead,hasAttachments:item.hasAttachments})), nextPageToken:data["@odata.nextLink"], total:data["@odata.count"] }; }
function trustedGraphNextLink(value:string) { const url = new URL(value); if (url.origin !== "https://graph.microsoft.com" || !url.pathname.startsWith("/v1.0/me/messages")) throw new Error("Página de e-mail inválida."); return url.toString(); }
function normalizeGoogleMessage(item:any): EmailMessage { const headers=Object.fromEntries((item.payload?.headers ?? []).map((h:any)=>[h.name.toLowerCase(),h.value])); return {id:item.id,provider:"google",threadId:item.threadId,from:{email:headers.from??""},to:[{email:headers.to??""}],cc:headers.cc?[{email:headers.cc}]:undefined,subject:headers.subject??"(sem assunto)",receivedAt:new Date(Number(item.internalDate)).toISOString(),snippet:item.snippet,bodyText:decodeGmailBody(item.payload),isUnread:(item.labelIds??[]).includes("UNREAD"),hasAttachments:(item.payload?.parts??[]).some((part:any)=>Boolean(part.filename))}; }
function normalizeMicrosoftMessage(item:any): EmailMessage { return {id:item.id,provider:"microsoft",threadId:item.conversationId,from:{email:item.from?.emailAddress?.address??"",name:item.from?.emailAddress?.name},to:(item.toRecipients??[]).map((x:any)=>({email:x.emailAddress.address,name:x.emailAddress.name})),cc:(item.ccRecipients??[]).map((x:any)=>({email:x.emailAddress.address,name:x.emailAddress.name})),subject:item.subject??"(sem assunto)",receivedAt:item.receivedDateTime,snippet:item.bodyPreview,bodyText:item.body?.content,isUnread:!item.isRead,hasAttachments:item.hasAttachments}; }
function decodeGmailBody(payload:any): string | undefined { const part = [payload,...(payload.parts??[])].find((item:any)=>item.mimeType?.startsWith("text/plain") && item.body?.data); return part?.body?.data ? Buffer.from(part.body.data,"base64url").toString("utf8") : undefined; }
function gmailAttachments(payload:any): EmailAttachment[] { const parts=[payload,...(payload.parts??[])]; return parts.filter((part:any)=>part.filename && part.body?.attachmentId).map((part:any)=>({id:part.body.attachmentId,name:part.filename,contentType:part.mimeType,size:part.body.size})); }

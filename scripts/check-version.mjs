import fs from "node:fs";import path from "node:path";import{fileURLToPath}from"node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const files=["package.json","packages/shared/package.json","packages/core/package.json","packages/browser-agent/package.json","apps/desktop/package.json"];const versions=files.map(file=>[file,JSON.parse(fs.readFileSync(path.join(root,file),"utf8")).version]);const expected=versions[0][1];const invalid=versions.filter(([,version])=>version!==expected);if(invalid.length)throw new Error(`Versões divergentes: ${JSON.stringify(versions)}`);console.log(`Versão sincronizada: ${expected}`);
